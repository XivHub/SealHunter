.vs/
.idea/
obj/
bin/
*.user
*.suo

# Local plan + agent scratch (per CLAUDE.md, not committed)
PLAN.md

# Local publish script (not committed)
publish.sh

# Local project memory for Claude Code (not committed)
CLAUDE.md
